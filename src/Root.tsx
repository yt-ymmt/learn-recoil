import React, { FC } from 'react';
import { SignedInRouter } from './Router';

const Root: FC = () => {
    return (
        <main>
            <SignedInRouter />
        </main>
    );
};

export default Root;
